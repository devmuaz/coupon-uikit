name: coupon_uikit
description: A UI kit that contains clippers and widgets which implements the shape of the coupon cards
version: 0.3.0
homepage: https://github.com/devmuaz/coupon_uikit

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
